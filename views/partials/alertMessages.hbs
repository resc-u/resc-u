
  <!-- information and successful messages stored in the User -->
  
  {{#if message }}
    <p class="message">{{ message }}</p>
  {{/if}}

  <!-- error messages -->

  {{#if error }}
    <p class="error_message">{{ error.message }}</p>
  {{/if}}